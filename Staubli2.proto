#RadTimeout = 100;
#PollPeriod = 100;
#ReplyTimeout = 2000;
#OutTerminator = "";
#InTerminator = "\r";
#ExtraInput = Ignore;

error
{ in "NACK: %(\$1:ERRCMD.VAL)s is not successful.";
}

#resetfault
#{ out "write_resetfault";
#  in "ACK:%s"; @mismatch{error;};
#}
#

power
{ out "power %d";
  in "ACK:%s"; @mismatch{error;};
}

gripper
{ out "gripper %d";
  in "ACK:%s"; @mismatch{error;};
}
